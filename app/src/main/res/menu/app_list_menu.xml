<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search"
        android:title="@string/action_search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:iconTint="?attr/colorOnSurface"
        app:showAsAction="ifRoom|collapseActionView" />

    <item
        android:id="@+id/action_filter"
        android:icon="@drawable/ic_filter_list"
        android:title="@string/action_filter"
        app:iconTint="?attr/colorOnSurface"
        app:showAsAction="never">
        <menu>
            <group
                android:id="@+id/group_filter"
                android:checkableBehavior="single">
                <item
                    android:id="@+id/filter_all"
                    android:checked="true"
                    android:title="@string/filter_all" />
                <item
                    android:id="@+id/filter_configured_only"
                    android:title="@string/filter_configured_only" />
                <item
                    android:id="@+id/filter_user_only"
                    android:title="@string/filter_user_only" />
                <item
                    android:id="@+id/filter_system_only"
                    android:title="@string/filter_system_only" />
            </group>
        </menu>
    </item>

    <item
        android:id="@+id/action_refresh"
        android:icon="@drawable/ic_refresh"
        android:title="@string/action_refresh"
        app:iconTint="?attr/colorOnSurface"
        app:showAsAction="never" />

</menu>
